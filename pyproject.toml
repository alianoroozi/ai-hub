[tool.poetry]
name = "ai-hub"
version = "0.1.0"
description = ""
authors = ["Ali Noroozi"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pre-commit = "^4.1.0"
pandas = "^2.2.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
